world
{
	start()
	{
		textln("This will count up from 0 to 9, waiting a little between numbers.");
		pause();
	
		local x = 0;
		while (x < 10)
		{
			text(x);
			wait(250);
			x = x + 1;
		}
		// Stick a newline at the end.
		text("\n");
		textln("Done!");
		quit;
	}
}
